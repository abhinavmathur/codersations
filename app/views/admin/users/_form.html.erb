<%= simple_form_for [:admin, @user] do |f| %>
    <%= f.input :name, required: true %>
    <%= f.input :email, required: true %>
    <%= f.input :password, required: true %>
    <%= f.input :admin, label: 'Tick box to make user an admin' %>
    <%= f.button :submit %>
<% end %>