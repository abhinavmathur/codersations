<%= simple_form_for [:admin, @category], input_html: { class: 'form-horizontal'} do |f| %>
    <%= f.input :name, label: false, placeholder: 'Name' %>
    <%= f.input :slug, label: false, placeholder: 'Slug name' %>
    <%= f.button :submit %>
<% end %>