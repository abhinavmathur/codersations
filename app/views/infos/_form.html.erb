<%= simple_form_for [@category, @tutorial, @info] do |f| %>
    <%= f.input :title, required: true, autofocus: true %>
    <%= f.input :content, class: 'summernote' %>
    <%= f.button :submit, class: 'btn btn-success' %>
<% end %>