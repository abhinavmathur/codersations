<%= simple_form_for [@category, @tutorial, @info, @note] do |f| %>
    <%= f.input :title, required: true, autofocus: true %>
    <%= f.input :content, required: true %>
    <%= f.input :task, as: :boolean %>
    <%= f.button :submit %>
<% end %>